import { ListDirective } from './list';
describe('List', () => {
  it('should create an instance', () => {
    const directive = new ListDirective(null as any);
    expect(directive).toBeTruthy();
  });
});
